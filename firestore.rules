rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The application uses a custom frontend authentication system
    // with hardcoded credentials. Firebase Auth is bypassed.
    // WARNING: This makes the database publicly readable and writable.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
